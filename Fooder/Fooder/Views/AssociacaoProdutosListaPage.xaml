<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Fooder.Views.AssociacaoProdutosListaPage"
             Title="AssociacaoProdutosListaPage">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="NOVO" Command="{Binding SalvarProdutosLista}">
            <ToolbarItem.Icon>
                <OnPlatform x:TypeArguments="FileImageSource" Android="plus.png" />
            </ToolbarItem.Icon>
        </ToolbarItem>
    </ContentPage.ToolbarItems>

    <StackLayout>
        <StackLayout Padding="10">
            <Picker x:Name="ComboListas" ItemsSource="{Bindig ListasGravadas}"  SelectedItem="{Binding ListaSelecionada, Mode=TwoWay}" />
        </StackLayout>
        <ScrollView>
            <ListView x:Name="LstProdutosCadastrados" ItemsSource="{Binding ListaProdutos}" ItemSelected="LstProdutosCadastrados_ItemSelected" IsRefreshing="{Binding Refresh}" RefreshCommand="{Binding GetRefreshedHistory}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <Label Text="{Binding NomeProduto}" VerticalTextAlignment="Center" HorizontalOptions="StartAndExpand" />
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

        </ScrollView>

    </StackLayout>
</ContentPage>